{
  "name": "ct574t-fullstack",
  "version": "1.0.0",
  "description": "MongoDB Sharded Cluster + Neo4j Social Network - Root package for development",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start-mongodb": ".\\script\\start-mongodb-cluster.ps1",
    "test-databases": "cd backend && node test-all-databases.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}